# Core pipeline
pydantic>=2.0
PyYAML>=6.0

# Identity Linker — PostgreSQL / Supabase connectivity
psycopg2-binary>=2.9

# Bronze-layer scrapers
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.1
icalendar>=5.0

# Parquet I/O
pandas>=2.0
pyarrow>=14.0
duckdb>=0.10

# Tier 3 — Spatial Resolver (point-in-polygon / containment / nearest)
# Shapely handles MultiPolygons, interior rings, polyline distance, and
# coordinate transforms correctly — replacing ad-hoc ray-cast helpers.
shapely>=2.0

# Expansion Agent + LLM Synthesis — geohash encoding and multi-provider LLM clients
geohash2>=1.1
openai>=1.0
anthropic>=0.25        # optional — used when ANTHROPIC_API_KEY is set
google-genai>=1.0      # optional — used when GEMINI_API_KEY is set

# Discovery Agent — web search (optional; LLM-only fallback when absent)
tavily-python>=0.3     # optional — used when TAVILY_API_KEY is set

# Realtor Agent chatbot UI
streamlit>=1.32

# Tier 4 — HTML report rendering for the Buildability Brief generator.
# Jinja2 ships transitively with streamlit / pip-tools, but Tier 4 imports
# it directly so we declare it explicitly.
jinja2>=3.1

# Tier 4 — Buildability Brief PDF export (optional; wkhtmltopdf fallback when absent)
playwright>=1.40

# TownEye Portal API
fastapi>=0.115
uvicorn[standard]>=0.32
httpx>=0.27
python-dotenv>=1.0
email-validator>=2.0

# Test suite
pytest>=7.4
pytest-mock>=3.12
